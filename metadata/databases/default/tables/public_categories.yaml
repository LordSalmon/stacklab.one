table:
  name: categories
  schema: public
array_relationships:
  - name: tool_categories
    using:
      foreign_key_constraint_on:
        column: category_id
        table:
          name: tool_categories
          schema: public
